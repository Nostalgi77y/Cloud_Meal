# 接口与演示说明

## 管理端接口

- `POST /api/auth/login`：管理员登录。
- `GET /api/admin/statistics/today`：今日经营数据。
- `GET /api/admin/dishes`：菜品列表。
- `POST /api/admin/dishes`：新增菜品。
- `PUT /api/admin/dishes/{id}`：修改菜品。
- `POST /api/admin/files/images`：管理员上传菜品图片，使用 `multipart/form-data` 的 `file` 字段，最大 5MB。
- `GET /api/admin/orders`：全部订单。
- `PUT /api/admin/orders/{id}/status/{status}`：订单状态流转。

## 用户端接口

- `POST /api/user/auth/demo-login`：演示用户登录。
- `POST /api/user/auth/register`：使用用户名或手机号 + 密码注册，并直接返回 JWT。
- `POST /api/user/auth/login`：正式账号密码登录。
- `GET /api/user/profile`：查询当前账号、userId 和个人资料。
- `PUT /api/user/profile`：修改当前账号昵称和头像。
- `GET /api/user/addresses`：查询当前用户收货地址。
- `POST /api/user/addresses`：新增收货地址。
- `PUT /api/user/addresses/{id}`：编辑收货地址。
- `PUT /api/user/addresses/{id}/default`：设为默认地址。
- `DELETE /api/user/addresses/{id}`：删除收货地址。
- `GET /api/user/coupons/available`：查询当前可领取优惠券。
- `POST /api/user/coupons/{couponId}/receive`：领取优惠券（每人每券限领一次）。
- `GET /api/user/coupons/mine?orderAmount=28`：查询我的优惠券及本单可用状态。
- `POST /api/user/orders`：提交订单；可传 `userCouponId`，优惠金额由服务端校验和计算。
- `DELETE /api/user/orders/{id}`：从当前用户的订单列表删除已完成或已取消订单；商家端仍保留订单记录。
- `POST /api/user/ai/conversations`：为当前 userId 创建 AI 会话。
- `GET /api/user/ai/conversations`：查询当前 userId 的 AI 历史。
- `GET /api/user/ai/conversations/{id}/turns`：查询会话轮次，同时校验用户归属。
- `POST /api/user/ai/analyze/text`：文字食材分析并写入当前用户会话记忆。
- `POST /api/user/ai/analyze`：图片/文字 multipart 分析。

所有 Java `Long` 类型业务 ID 在 JSON 中统一返回字符串，避免微信小程序和浏览器发生 JavaScript 大整数精度丢失。
- `GET /api/user/categories`：在用分类。
- `GET /api/user/dishes?categoryId=1`：在售菜品。
- `GET/POST/DELETE /api/user/cart`：购物车。
- `GET /api/user/addresses`：地址列表。
- `POST /api/user/orders`：提交订单。
- `GET /api/user/orders`：用户订单。
- `POST /api/user/orders/{id}/mock-pay`：模拟支付。

完整结构和在线调试以 Swagger UI 为准。

## 推荐演示流程

1. 启动基础设施、后端和两个前端。
2. 商家端使用默认管理员登录，展示菜品和经营概览。
3. 小程序选择分类并将两个菜品加入购物车。
4. 提交订单并模拟支付。
5. 商家端收到 WebSocket 提醒并刷新订单。
6. 依次执行接单、制作完成、开始配送和完成。
7. 小程序订单页重新进入后展示最新状态。
8. 用同一 `clientOrderNo` 重复提交，说明服务端幂等设计。
9. 创建订单后不支付，展示 RabbitMQ 超时关单设计（演示时可临时缩短队列 TTL）。
