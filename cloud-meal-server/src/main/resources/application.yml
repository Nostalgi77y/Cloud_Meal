spring:
  application:
    name: cloud-meal-server
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  jackson:
    default-property-inclusion: non_null
    time-zone: Asia/Shanghai
  flyway:
    enabled: true
    locations: classpath:db/migration
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

springdoc:
  swagger-ui:
    path: /swagger-ui.html

cloud-meal:
  upload-dir: ${FILE_UPLOAD_DIR:./uploads}
  jwt:
    secret: ${JWT_SECRET:cloud-meal-dev-secret-key-change-before-production-2026}
    expiration: ${JWT_EXPIRATION:7200}
  wechat:
    app-id: ${WECHAT_APP_ID:}
    app-secret: ${WECHAT_APP_SECRET:}
  payment:
    mode: ${PAYMENT_MODE:MOCK}
    merchant-id: ${WECHAT_PAY_MERCHANT_ID:}
    merchant-serial-number: ${WECHAT_PAY_MERCHANT_SERIAL_NUMBER:}
    api-v3-key: ${WECHAT_PAY_API_V3_KEY:}
    private-key-path: ${WECHAT_PAY_PRIVATE_KEY_PATH:}
    notify-url: ${WECHAT_PAY_NOTIFY_URL:}
  ai-chef:
    base-url: ${AI_CHEF_BASE_URL:http://127.0.0.1:8081}
    service-token: ${CLOUD_MEAL_AI_SERVICE_TOKEN:}
    connect-timeout-ms: ${AI_CHEF_CONNECT_TIMEOUT_MS:3000}
    read-timeout-ms: ${AI_CHEF_READ_TIMEOUT_MS:70000}
